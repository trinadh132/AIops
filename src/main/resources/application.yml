server:
  port: 8080

spring:
  application:
    name: self-healing-ops-mock-service

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.opsagent.mock: INFO
